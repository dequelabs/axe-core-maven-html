name: Create Project Issue

on: push
jobs:
  create-project-issue:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dequelabs/axe-api-team-public/.github/actions/create-project-issue-v1@3c732ba51137271ffcf15c584bebc03a5f05f9db
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          title: "I am a new issue, hazaar!"
          body: "I am new issue body, also hazaar!"
          project_id: 66
